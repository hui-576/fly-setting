# 视距通信规划

该领域描述在给定任务点和地形条件下，为单架系留无人机选择升空位置与高度，以建立应急现场的视距通信覆盖。

## Language

**任务点**:
必须获得通信覆盖的点状地理位置，具有名称和坐标。
_Avoid_: 任务区域、保障面、通信点位

**升空限制区域**:
允许系留无人机部署的地理范围，由系统根据道路生成或由网络工程师手绘确定。
_Avoid_: 禁飞区、覆盖区域、搜索区域

**可升空点**:
升空限制区域经过网格或道路中心离散化后形成的、尚未完成覆盖能力判定的位置。
_Avoid_: 候选升空点、无人机点位

**候选升空点**:
在允许高度范围内达到规定任务点覆盖档位的可升空点，包含推荐升空高度和覆盖结果。
_Avoid_: 可升空点、最终部署点

**推荐升空高度**:
候选升空点达到对应覆盖档位所需的最低当地地面以上高度。
_Avoid_: 飞行高度、海拔高度

**任务点天线高度**:
任务点通信天线相对该点当地地面的垂直高度，由网络工程师对每个任务点明确填写。
_Avoid_: 任务点海拔、地表高程、默认终端高度

**任务点覆盖率**:
从给定升空位置和高度具有视距关系的任务点数量占全部任务点数量的比例。
_Avoid_: 地理覆盖率、面积覆盖率、信号强度

**部分覆盖阈值**:
网络工程师按规划任务设置的黄色候选档位覆盖比例边界；候选点的实际任务点覆盖率必须严格大于该值。
_Avoid_: 固定 70% 档位、100% 覆盖档位、面积覆盖率

**视距通信覆盖范围**:
以候选升空点和推荐升空高度为观察位置，在规定半径内按地形遮挡判定出的可视与不可视地理区域。
_Avoid_: 任务点覆盖率、无线信号覆盖范围

**覆盖图接收端高度**:
连续覆盖范围中所有地图位置统一采用的接收端当地地面以上高度，按规划任务配置。
_Avoid_: 任务点天线高度、无人机升空高度、海拔高度

**裸地高程模式**:
使用不包含建筑物和植被高度的裸地高程模型判断视距遮挡，只表达山体和地形影响。
_Avoid_: 地表高程模式、完整障碍模式

**地表高程模式**:
使用包含建筑物、植被等地表对象高度的地表高程模型判断视距遮挡。
_Avoid_: 裸地高程模式、纯地形模式

**有效地球半径系数**:
用于在视距计算中等效表示地球曲率与大气折射影响的无量纲参数，标准默认值为 `4/3`，可按任务调整。
_Avoid_: 地球半径、折射率、固定常量

**计算检查点**:
全量候选点搜索完成一个确定性批次后保存的可校验续算状态，不是可供部署决策使用的部分候选结果。
_Avoid_: 临时结果、有效结果、历史版本

**网络工程师**:
负责设置规划条件、审查候选升空点并完成现场部署决策的系统主要用户。
_Avoid_: 普通用户、无人机操作员、系统管理员
