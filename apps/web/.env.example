# 默认通过 GET /api/v1/map/manifest 读取本地数据包；离线图层不使用 Vite 构建变量。
VITE_MAP_PROVIDER=offline

# 仅开发调试时在未提交的 .env.local 中显式配置，严禁提交真实 Key。
# VITE_MAP_PROVIDER=tianditu
# VITE_TDT_KEY=
# VITE_DEV_TERRAIN_URL=https://example.test/terrain
